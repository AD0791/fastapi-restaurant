services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    volumes:
      - .:/app
      - /app/.venv
    command: sh -c 'exec uv run uvicorn main:app --host 0.0.0.0 --port $${PORT:-8000} --reload'
